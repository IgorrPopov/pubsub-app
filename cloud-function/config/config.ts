const projectId = 'test-pub-sub-app';
const instanceId = 'pubsub-test-app'; 
const databaseName = 'pubsub-test-app';

const topicName = 'cloud-function-response-to-pubsub';

export { projectId, instanceId, databaseName, topicName };
